<h3>新增回覆</h3>
<%= error_messages_for :comment %>
<% form_for(:comment, :url => comments_path(:user_id => @entry.user, :entry_id => @entry)) do |f| -%>
	<p><%= f.text_area :body, :rows => Comment::BODY_ROWS, :cols => Comment::BODY_COLS, :class => 'mceEditor' %></p>
	<div>
		<%= submit_tag '送出回覆', :class => 'button' -%>
	</div>
<% end -%>

