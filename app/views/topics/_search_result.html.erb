<li>
  <div class="profile-image">
    <%= image_link search_result.user, :image => :small %>
    <%= image_tag "icons/comment.png", :class => "icon" %>
  </div>
  <p >
    在 <%= link_to(h(search_result.topic.title),
                  forum_topic_path(search_result.topic.forum, search_result.topic,
                                   :anchor => "post_#{search_result.id}")) %>
    討論主題：
  </p>
  <div class="forum">
  <%= display search_result.body %>
  </div>
</li>