<% if user.status
  status = user.status.text
  time = user.status.created_at
else
  status = "更新你目前的狀態"
  time = Time.now
end -%>
<span><strong><%= user.short_name %></strong></span> 
<span id="current-status-text"><%= status %></span>
<small>
  <span id="status-time" class="status-time"><%= time_ago_in_words(time) %> 前</span>
  <span id="status-clear">
    <% if user.status -%>
      - <%= link_to_remote("清除", :url => user_status_update_path(user, user.status), :method => :delete, :class => "status-clear-link") %>
    <% end -%>    
  </span>        
</small>