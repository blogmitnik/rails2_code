<h3><%= @user.name %> 的相片標籤</h3>

<p><%= link_to "顯示所有人的相片標籤", tags_path %></p>

<% tag_cloud @tags, %w(tag1 tag2 tag3 tag4 tag5) do |name, css_class| %>
	<%= link_to name, tag_path(name), :class => css_class %>
<% end %>