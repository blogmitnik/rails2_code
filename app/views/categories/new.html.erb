<h2>新增文章分類</h2>
<%= error_messages_for :category %>
<% form_for(:category, :url => categories_path) do |f| -%>
	<p>分類名稱:<br><%= f.text_field :name, :size => 60 %></p>
	<%= submit_tag '建立' %> or <%= link_to '取消', admin_categories_url %>
<% end %>